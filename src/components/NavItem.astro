---

const {title, href} = Astro.props;
---

<a href={href}>
  <slot></slot>
  {title}
</a>

<style>
  a{
    display: flex;
    flex-direction: column;
    color: var(--primary-color);
    align-items: center;
    height: 3rem;
    width: 3rem;
  }
</style>