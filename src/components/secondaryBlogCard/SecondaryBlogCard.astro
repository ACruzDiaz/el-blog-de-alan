---
const {primeraCategoria, image, title, slug} = Astro.props;


---



    <a href={'/blog/'+slug} class="secondaryBlogCard" >
      <img src={image} alt={'tarjeta' + primeraCategoria}>
      <span>{primeraCategoria}</span>
      <h3>{title}</h3>
    </a>



<style>
  .secondaryBlogCard{
    position: relative;
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    height: 174px;
    min-width: 200px;
    flex-basis: 20%;

    img{
      width: 100%;
      height: 60%;
      object-fit: cover;
      margin-bottom: 0.5rem;
    }
    span{
      position: absolute;
      top: 90px;
      background-color: var(--accent-color);
      color: var(--secondary-color);
      padding: 0.125rem 0.5rem;
      font-size: var(--font-small);
    }
    h3{
      overflow-wrap: anywhere;
      text-overflow: clip;
    }
  }
</style>